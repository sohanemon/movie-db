'use client';
import { ReactSVG } from 'react-svg';

export default function TopBar() {
  return (
    <section>
      <ReactSVG src={`/assets/images/search.svg`} width={24} height={24} />
    </section>
  );
}
